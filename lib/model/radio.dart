class RadioModel {
  bool isSelected;
  String groupValue;
  String text;
  String image;
  String languageCode;

  RadioModel(
      this.isSelected, this.groupValue, this.text, this.image, this.languageCode);
}
